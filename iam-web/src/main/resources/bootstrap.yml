spring:
  application:
      name: my-iam
  profiles:
    active: "local"

---
spring:
  profiles: local
  cloud:
    config:
      uri: http://config.pas-mini.io

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://marathon-lb-internal.marathon.mesos:10000